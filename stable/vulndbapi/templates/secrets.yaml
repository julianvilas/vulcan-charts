apiVersion: v1
kind: Secret
metadata:
  labels:
    name: {{ include "vulndbapi.fullname" . }}
  name: {{ include "vulndbapi.fullname" . }}
type: Opaque
data:
  PG_PASSWORD: {{ .Values.db.password | b64enc | quote }}
